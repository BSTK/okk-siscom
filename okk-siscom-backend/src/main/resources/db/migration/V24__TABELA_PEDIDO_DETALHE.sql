CREATE TABLE PEDIDO_DETALHE
(
    QUANTIDADE              NUMBER         NOT NULL,
    VALOR_UNITARIO          DECIMAL(10, 2) NOT NULL,
    VALOR_TOTAL             DECIMAL(10, 2) NOT NULL,
    DATA_INSERT             TIMESTAMP      NOT NULL,
    DATA_UPDATE             TIMESTAMP      NOT NULL,
    PEDIDO_CABECALHO_ID     NUMBER         NOT NULL,
    COTACAO_ID              NUMBER         NOT NULL,
    REQUISICAO_CABECALHO_ID NUMBER         NOT NULL,
    PRODUTO_ID              NUMBER         NOT NULL,
    PRIMARY KEY (PEDIDO_CABECALHO_ID, COTACAO_ID, REQUISICAO_CABECALHO_ID, PRODUTO_ID),
    FOREIGN KEY (PEDIDO_CABECALHO_ID) REFERENCES PEDIDO_CABECALHO (ID),
    FOREIGN KEY (COTACAO_ID) REFERENCES COTACAO (ID),
    FOREIGN KEY (REQUISICAO_CABECALHO_ID) REFERENCES REQUISICAO_CABECALHO (ID),
    FOREIGN KEY (PRODUTO_ID) REFERENCES PRODUTO (ID)
);